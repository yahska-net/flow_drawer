/// drawer postion enum to identify where should the drawer items list shown
enum FlowDrawerPosition {
  /// items falls to the left side
  left,

  /// items falls to the right
  right,
  // top, bottom can be added in future
}
